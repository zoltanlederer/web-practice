> 🚧 Work in progress

- User registration + login (username/email + password)
- Password hashing (bcrypt)
- JWT (JSON Web Token)
- At least one protected route (e.g. only a logged-in user can create/delete movies) that returns 401 when there's no valid credential